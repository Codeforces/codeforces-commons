name: Publish package to GitHub Packages
on:
  push:
  pull_request:

jobs:
  ci:
    runs-on: windows-latest
    permissions:
      contents: read
      packages: write
    env:
      MAVEN_OPTS: "--illegal-access=permit --add-opens java.base/java.lang=ALL-UNNAMED --add-opens java.base/java.util=ALL-UNNAMED --add-opens java.base/java.io=ALL-UNNAMED"
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v2
        with:
          java-version: "17"
          distribution: "adopt"
      - name: Validate
        run: |
          cd code
          mvn --batch-mode validate
      - name: Test
        run: |
          cd code
          mvn --batch-mode test
      - name: Publish
        if: github.ref == 'refs/heads/master' && github.repository_owner == 'Codeforces'
        run: |
          cd code
          mvn --batch-mode deploy -DskipTests
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
